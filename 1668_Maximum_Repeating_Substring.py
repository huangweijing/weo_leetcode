class Solution:
    def maxRepeating(self, sequence: str, word: str) -> int:
        idx = 0
        while idx < len(str):
            matched = True
            for i, ch in word:
                if ch == sequence
            idx += 1
